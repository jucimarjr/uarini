-class(pingpong).
-constructor([new/1]).

class_methods.

new() ->
	spawn(?CLASS, pong, []).

pong() ->
    receive
        {From, D} ->
            From ! {self(), D},
            pong()
    end.
