-class(cat).
-extends(animal).
-export([falar/1, correr/1, accept/1]).

falar(Text) ->
	io:format("~w~n", [Text]).

correr(Text) ->
	io:format("~w~n", [Text]).

accept(visitor Visitor) ->
	Visitor::visit(self).
