-class(stdoutLogger).
-extends(logger).
-constrcutor([constructor/1]).

class_methods.

constrcutor(Mask) ->
	self::Mask = Mask.

protected writeMessage(Msg) ->
	io:format("Writing stdout: ~p", [msg]).
