-class(simpleRemoteControl).
-export([set_command/1, button_was_pressed/0]).

attributes.

Slot.

methods.

set_command(Command) ->
	self::Slot = Command.
	
button_was_pressed() ->
	Temp = self::Slot,
	Temp::execute().