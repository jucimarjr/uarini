-class(area).
-constructor([new/1]).
-export([area/1]).

class_atribbuttes.

R = NULL
J = 0.234
S = "teste"
K = 4
Z = NULL


class_methods.

new([]) -> 
	spawn fun thread().

print() -> 
	A = self::R.

thread() ->
	receive
		{ From, { area, [Dados] } } ->
			From ! area(Dados),
			thread();
		
		{ From, { print, [] } } ->
			From ! print(),
			thread();
		
		{ From, Other } ->
			From ! { self(), { error, Other } },
			thread()
	end.

area(Dados)->
	ok.
