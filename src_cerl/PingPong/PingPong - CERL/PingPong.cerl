-class(PingPong).
-export([spawn_process/2, pong/1]).

class_methods.

spawn_process(Method, Parameters) ->
	spawn(?CLASS, Method, Parameters).

pong() ->
    receive
        {From, D} ->
            From ! {self(), D},
            pong()
    end.
