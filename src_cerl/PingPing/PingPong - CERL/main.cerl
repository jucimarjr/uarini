class(Main).
-import([Ping]).
-export([run/2]).

class methods.

run(R, D) ->
	P1 = Ping::new(pingping, [D]),
	P2 = Ping::new(pingping. [D]),
	P1 ! {init, self(), P2, R},
    P2 ! {init, self(), P1, R}.
